# PDF extraction
pymupdf==1.24.5

# OCR engines
pytesseract==0.3.10
# paddleocr doesn't declare paddlepaddle as a pip dependency (CPU/GPU builds
# differ), so it must be pinned separately. paddleocr 2.7.3 targets the
# PaddlePaddle 2.x API -- pin to that generation, not the newer 3.x line.
paddlepaddle==2.6.2
paddleocr==2.7.3
easyocr==1.7.1
# paddleocr hard-requires opencv-python<=4.6.0.66 and opencv-contrib-python<=4.6.0.66
# in addition to opencv-python-headless, so all three end up installed side by side
# (they share the same importable `cv2` module). Pin all three to the same version
# so whichever one wins on disk doesn't introduce an API-version mismatch.
opencv-python-headless==4.6.0.66
opencv-python==4.6.0.66
opencv-contrib-python==4.6.0.66
Pillow==10.3.0

# RAG / vector index
llama-index==0.10.40
llama-index-vector-stores-faiss==0.1.2
llama-index-embeddings-huggingface==0.2.1
llama-index-llms-gemini==0.1.11
faiss-cpu==1.8.0
sentence-transformers==3.0.1

# LLM clients
mistralai==0.4.2
google-generativeai==0.5.4
transformers==4.41.2
accelerate==0.30.1
torch==2.3.0

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.2

# Utilities
python-dotenv==1.0.1
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.4
loguru==0.7.2

# Evaluation
scikit-learn==1.5.0
rouge-score==0.1.2

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
